version: "3.3"

services:
  openbooks:
    image: evanbuss/openbooks
    container_name: openbooks
    restart: unless-stopped
    ports:
      - "8083:80"
    volumes:
      - /mnt/docker/openbooks:/books
